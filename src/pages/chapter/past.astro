---
// История аукционов теперь встроена в /codex?tab=chapter (кнопка "История
// аукционов" переключает фильтр в общем расписании). Отдельная страница
// удалена, редирект сохраняет старые внешние ссылки/закладки рабочими.
export const prerender = false;
return Astro.redirect('/codex?tab=chapter', 301);
---
