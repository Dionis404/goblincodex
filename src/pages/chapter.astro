---
// Глава живёт только внутри таба /codex?tab=chapter — отдельная страница
// удалена, чтобы не дублировать контент (баннер, аукционы, механики).
// Редирект сохраняет старые внешние ссылки/закладки рабочими.
export const prerender = false;
return Astro.redirect('/codex?tab=chapter', 301);
---
