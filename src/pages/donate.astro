---

---

<html lang="ru">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Поддержать гоблинов | GoblinCodex</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet" />
		<style>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}

			html, body {
				width: 100%;
				height: 100%;
			}

			body {
				background-color: #2a2d45;
				background-image: url('/images/bg.png');
				background-size: cover;
				background-attachment: fixed;
				background-position: center;
				min-height: 100vh;
				display: flex;
				align-items: center;
				justify-content: center;
				overflow: auto;
				padding: 20px;
			}

			.container {
				display: flex;
				flex-direction: column;
				align-items: center;
				justify-content: center;
				text-align: center;
				max-width: 700px;
				padding: 40px;
				background-color: rgba(42, 45, 69, 0.85);
				border-radius: 10px;
				backdrop-filter: blur(10px);
			}

			@keyframes fadeUp {
				from {
					opacity: 0;
					transform: translateY(30px);
				}
				to {
					opacity: 1;
					transform: translateY(0);
				}
			}

			.back-button {
				align-self: flex-start;
				background: none;
				border: none;
				color: #ffac31;
				font-size: 1rem;
				cursor: pointer;
				margin-bottom: 20px;
				font-family: Arial, sans-serif;
				transition: all 0.3s ease;
				text-decoration: none;
				padding: 5px 10px;
			}

			.back-button:hover {
				color: #ffeed6;
				transform: translateX(-5px);
			}

			.title {
				font-family: 'Press Start 2P', cursive;
				font-size: 2.5rem;
				color: #ffac31;
				letter-spacing: 3px;
				margin-bottom: 10px;
				line-height: 1.2;
				animation: fadeUp 0.8s ease-out 0.1s both;
			}

			.subtitle {
				color: rgba(245, 190, 112, 0.84);
				font-size: 0.95rem;
				font-family: Arial, sans-serif;
				margin-bottom: 30px;
				animation: fadeUp 0.8s ease-out 0.2s both;
				letter-spacing: 0.5px;
			}

			.divider {
				width: 150px;
				height: 1px;
				background-color: #ffeed6;
				margin: 20px auto 30px;
				animation: fadeUp 0.8s ease-out 0.3s both;
			}

			.donate-section {
				display: flex;
				flex-direction: column;
				gap: 25px;
				animation: fadeUp 0.8s ease-out 0.4s both;
			}

			.donate-item {
				background-color: rgba(109, 62, 143, 0.3);
				border: 2px solid #6d3e8f;
				border-radius: 8px;
				padding: 20px;
				transition: all 0.3s ease;
			}

			.donate-item:hover {
				background-color: rgba(109, 62, 143, 0.5);
				border-color: #ffac31;
				transform: translateY(-5px);
				box-shadow: 0 10px 30px rgba(255, 172, 49, 0.2);
			}

			.donate-title {
				color: #ffac31;
				font-size: 1.1rem;
				font-family: Arial, sans-serif;
				font-weight: bold;
				margin-bottom: 10px;
				letter-spacing: 1px;
			}

			.donate-link {
				color: #ffeed6;
				font-size: 0.9rem;
				font-family: 'Courier New', monospace;
				word-break: break-all;
				background-color: rgba(0, 0, 0, 0.2);
				padding: 10px;
				border-radius: 4px;
				margin-bottom: 10px;
				line-height: 1.6;
				user-select: all;
			}

			.donate-link a {
				color: #ffeed6;
				text-decoration: none;
				transition: color 0.3s ease;
			}

			.donate-link a:hover {
				color: #ffac31;
			}

			.copy-button {
				background-color: #6d3e8f;
				color: #ffeed6;
				border: 1px solid #ffeed6;
				padding: 8px 16px;
				border-radius: 20px;
				font-size: 0.85rem;
				font-family: Arial, sans-serif;
				cursor: pointer;
				transition: all 0.3s ease;
			}

			.copy-button:hover {
				background-color: #8b5bb5;
				transform: scale(1.05);
			}

			.copy-button:active {
				transform: scale(0.95);
			}

			.networks {
				font-size: 0.85rem;
				color: rgba(245, 190, 112, 0.84);
				margin-top: 10px;
				font-style: italic;
			}

			.thanks {
				margin-top: 30px;
				animation: fadeUp 0.8s ease-out 0.5s both;
				color: #ffeed6;
				font-size: 1rem;
				line-height: 1.8;
				font-family: Arial, sans-serif;
			}

			.thanks p {
				margin-bottom: 10px;
			}

			@media (max-width: 768px) {
				.container {
					padding: 20px;
				}

				.title {
					font-size: 1.8rem;
				}

				.donate-link {
					font-size: 0.8rem;
				}
			}
		</style>
	</head>
	<body>
		<div class="container">
			<a href="/" class="back-button">← Назад</a>

			<h1 class="title">Поддержи</h1>
			<p class="subtitle">Помогите гоблинам продолжить разработку</p>

			<div class="divider"></div>

			<div class="donate-section">
				<div class="donate-item">
					<div class="donate-title">💳 Донат картой</div>
					<div class="donate-link">
						<a href="https://pay.cloudtips.ru/p/1d436e37" target="_blank" rel="noopener noreferrer">
							https://pay.cloudtips.ru/p/1d436e37
						</a>
					</div>
					<button class="copy-button" onclick="copyToClipboard('https://pay.cloudtips.ru/p/1d436e37')">
						Скопировать ссылку
					</button>
				</div>

				<div class="donate-item">
					<div class="donate-title">💎 Крипто</div>
					<div class="donate-link" id="crypto-address">
						0xf292959d216CB539Dc84E8F6cb9f11F55D877E32
					</div>
					<button class="copy-button" onclick="copyToClipboard('0xf292959d216CB539Dc84E8F6cb9f11F55D877E32')">
						Скопировать адрес
					</button>
					<div class="networks">
						Сети: Base / ETH / Polygon
					</div>
				</div>
			</div>

			<div class="thanks">
				<p>🙏 Спасибо за поддержку!</p>
				<p>Вы помогаете гоблинам работать быстрее и создавать лучший контент</p>
			</div>
		</div>

		<script>
			function copyToClipboard(text) {
				navigator.clipboard.writeText(text).then(() => {
					alert('Скопировано в буфер обмена! ✓');
				}).catch(() => {
					// Fallback для старых браузеров
					const textarea = document.createElement('textarea');
					textarea.value = text;
					document.body.appendChild(textarea);
					textarea.select();
					document.execCommand('copy');
					document.body.removeChild(textarea);
					alert('Скопировано в буфер обмена! ✓');
				});
			}
		</script>
	</body>
</html>
