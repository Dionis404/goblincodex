services:
  astro:
    image: ghcr.io/dionis404/goblincodex:latest
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - shared-net
    restart: unless-stopped

networks:
  shared-net:
    external: true
