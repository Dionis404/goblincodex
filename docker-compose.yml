services:
  astro:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4321:4321"
    env_file:
      - .env
    networks:
      - shared-net
    restart: unless-stopped

networks:
  shared-net:
    external: true